{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npx drizzle-kit push --force && npx tsx lib/db/seed.ts && next start -p ${PORT:-3000}",
    "restartPolicyType": "ON_FAILURE"
  }
}
