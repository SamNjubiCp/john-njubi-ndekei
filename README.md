# John Njubi Ndekei

Memorial site: life story, lessons, hymns, family tree, burial livestream.

```bash
cp .env.example .env
npm install
npm run db:up
npm run db:push
npm run db:seed
npm run dev
```

Keeper admin: `/admin/login` using `ADMIN_SECRET`.
